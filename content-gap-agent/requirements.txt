# Content Gap Agent — Python Dependencies
# Install with: pip install -r requirements.txt

# --- Web Scraping ---
firecrawl-py>=1.5.0          # Firecrawl SDK for structured web crawling

# --- AI / LLM ---
openai>=1.30.0               # OpenAI SDK (embeddings + GPT completions)

# --- Data Handling ---
pandas>=2.1.0                # DataFrame operations, CSV export
numpy>=1.26.0                # Numerical operations for embeddings

# --- Machine Learning ---
scikit-learn>=1.4.0          # cosine_similarity for embedding comparison

# --- Configuration ---
pyyaml>=6.0.1                # Parse sites.yaml configuration

# --- Scheduling ---
apscheduler>=3.10.4          # APScheduler — BlockingScheduler + CronTrigger

# --- HTTP / Slack / Semrush / Notion ---
requests>=2.31.0             # Slack webhook + Semrush API calls
notion-client>=2.2.1         # Notion SDK for exporting generated scripts

# --- Environment ---
python-dotenv>=1.0.0         # Load .env files

# --- Web Server (Vercel/Railway deployment) ---
fastapi>=0.110.0             # ASGI web framework (routes, SSE streaming)
uvicorn>=0.27.0              # ASGI server for Railway/Render

# --- Development / Testing (optional) ---
pytest>=8.0.0                # Test runner
pytest-mock>=3.12.0          # Mocking for tests
